<h1>All the Missing Pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li><%= "#{pet.name} is a #{pet.species} and was found #{pet.found_days_ago} days ago near #{pet.address}" %></li>
  <% end %>
</ul>

